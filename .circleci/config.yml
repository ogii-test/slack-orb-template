version: '2.1'
 
orbs:
  slack: circleci/slack@4.2.1

#this is master

jobs:
  job1:
    docker:
      - image: 'cimg/base:2020.01'  
    steps:
      #- checkout
      - run:
          command: |
            exit 1
      - run:
          command: |
            echo 'export MY_ORB_TEMPLATE_PASS=$(jq .pass ./slack_template/templates.json)' >> $BASH_ENV
            echo "$MY_ORB_TEMPLATE_PASS"
      - run:
          command: |
            echo 'export MY_ORB_TEMPLATE_FAIL=$(jq .fail ./slack_template/templates.json)' >> $BASH_ENV
            echo "$MY_ORB_TEMPLATE_FAIL"
      - slack/notify:
          channel: slacktest
          event: pass
          template: MY_ORB_TEMPLATE_PASS
      - slack/notify:
          channel: slacktest
          event: fail
          template: MY_ORB_TEMPLATE_FAIL
  job2:
    docker:
      - image: 'cimg/base:2020.01' 
    steps:
      - add_ssh_keys:
          fingerprints:
            - "78:32:ef:99:39:64:56:ea:8e:73:82:9f:2a:70:4d:3f"
      - checkout
      - run:
          command: |
            exit 0
            - checkout
      - run:
          command: |
            echo 'export MY_ORB_TEMPLATE_PASS=$(jq .pass ./slack_template/templates.json)' >> $BASH_ENV
            echo "$MY_ORB_TEMPLATE_PASS"
      - run:
          command: |
            echo 'export MY_ORB_TEMPLATE_FAIL=$(jq .fail ./slack_template/templates.json)' >> $BASH_ENV
            echo "$MY_ORB_TEMPLATE_FAIL"
      - slack/notify:
          channel: slacktest
          event: pass
          template: MY_ORB_TEMPLATE_PASS
      - slack/notify:
          channel: slacktest
          event: fail
          template: MY_ORB_TEMPLATE_FAIL

workflows:
  send-notification:
    jobs:
      - job1:
          context: slack-secrets
          filters:
            branches:
              only: /test.*/
      - job2:
          context: slack-secrets
